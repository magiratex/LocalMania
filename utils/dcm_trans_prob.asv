function G = dcm_trans_prob(G, ind, w, attr)

E = size(G, 1);

%% randomly generate transition probability
for i = 1 : E
%     N = sum(G(i, :));
%     pr = rand(1, N);
%     pr = pr ./ sum(pr);
%     G(i, G(i,:)==1) = pr;
    I = find(G(i, :) == 1);
    for j = 1 : length(I)
        I(j)
    end;
end;

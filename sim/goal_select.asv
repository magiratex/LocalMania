function gid = goal_select(G, agt)

gid = -1;

if strcmp(agt.state, 'gs')
    connected = find(G(agt.gid, :) == 1);
    
    while 1
        gid = randi(length(connected)); % naive model
        gid = connected(gid);
        if gid ~= agt.seq(end) && lenght(agt.seq)==1
            break;
            
            if lenght(agt.seq)>1 && 
            end;
        end;
    end;
end;